<h1>Users#edit</h1>

<%# Using the same logic as the meals edit, you can borrow the signup form and use it here... %>

<%= form_tag sessions_path,{:class => 'form-horizontal'} do %>
	    <div class="form-group">
	        <%= label_tag :email, 'Email', class: 'col-md-4 control-label' %>
		    <div class="col-md-5">
		        <%= text_field_tag :email, params[:email], class: 'form-control' %>
		    </div>
	    </div>
	    <div class="form-group">
	        <%= label_tag :password, 'Password', class: 'col-md-4 control-label' %>
		    <div class="col-md-5">
		        <%= password_field_tag :password, nil, class: 'form-control' %>
	    </div>
    </fieldset>

<%= link_to("Delete Account", user_path(current_user), method: 'delete') %>